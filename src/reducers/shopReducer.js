import { SHOPS } from "../actions/types";

const initialState = {
  shops: {}
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
